version: '3'
services:
  accelerator:
    build: .
    container_name: gtun
    restart: always
    network_mode: host
    privileged: true
    volumes:
      - /opt/apps/gtun/logs:/opt/apps/gtun/logs
    environment:
      TIME_ZONE: Asia/Shanghai